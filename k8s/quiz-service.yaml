apiVersion: v1
kind: Service
metadata:
  name: quiz-service
  labels:
    app: quiz-service
spec:
#  type: ClusterIP => should be this
  type: NodePort
  ports:
    - name: "quiz-service"
      port: 8081
      targetPort: 8081
      nodePort: 30081
  selector:
    app: quiz-service
