apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
spec:
#  type: ClusterIP => should be this
  type: NodePort
  ports:
    - name: "db-service"
      port: 5432
      targetPort: 5432
      nodePort: 30432
  selector:
    app: db
